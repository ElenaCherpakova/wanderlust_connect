<nav class="navbar navbar-expand-lg sticky-top">
  <div class="container-fluid">
      <a class="navbar-link" href="<%= root_path %>">WanderlustConnect</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation" style="background-color: #e3f2fd;">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
         <% if user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%= dashboard_path %>">Dashboard</a>
        </li>
        <%end%>
      </ul>
      </form>
      <div class="d-flex justify-content-between mt-2 align-items-baseline ">
        <% if user_signed_in? %>
          <p>Welcome, <strong><%= current_user.email %></strong>!</p>
          <%=button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-light ms-2"%>
        <% else %>
          <%= link_to "Sign up", new_user_registration_path, class: "btn btn-outline-light" %>
          <%= link_to "Sign in", new_user_session_path, class: "btn btn-outline-light mx-2" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>